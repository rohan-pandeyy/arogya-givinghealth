import React from 'react';
import {View, StyleSheet} from 'react-native';

const ModelViewer = () => {
  return <View style={styles.modelContainer} />;
};

const styles = StyleSheet.create({
  modelContainer: {
    flex: 2,
  },
});

export default ModelViewer;